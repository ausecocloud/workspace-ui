// import { createSelector } from 'resdlect';

export const userSelector = state => state.user;

export const projectsSelector = state => state.projects;

export const contentsSelector = state => state.contents;
